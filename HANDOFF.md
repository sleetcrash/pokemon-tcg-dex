# HANDOFF

Working notes for continuing development. Public repo: keep this file free of personal details.

## What this is

Single-file web app (`index.html`) tracking a Pokemon TCG National Card Dex: 1,602 entries covering all 1,025 species plus regional forms, other form variations, megas, Gigantamax, and TCG-only states (Dynamax, Tera). Static hosting on Vercel (linked to this repo) with one serverless function (`api/tcg.js`) that proxies the TCGdex card API. GitHub Pages also serves `main` but Vercel is the intended production host.

## Repo state at handoff

- `main`: last known-good build before Dynamax/Tera and the card lookup.
- `MAX-forms` branch (open pull request): adds Gigantamax, Dynamax, Tera, Surfing/Flying Pikachu, the card lookup sheet, the same-origin proxy (`api/tcg.js`), and a pokemontcg.io fallback provider (see below). Everything is committed; nothing lives only in a local build.

## Architecture (as built)

- One HTML file: CSS, markup, and JS inline. Data embedded as JS constants:
  - `DEX`: array of entries `[dexNo, id, name, region, gen, formType, spriteRefId?]`. Ids are stable and are the localStorage keys; never renumber.
  - `SPR`: sprite PNGs as base64, keyed by id. Dynamax/Tera/Surfing/Flying reuse another entry's sprite via the 7th field.
  - `RK`: regional dex numbers per region (from PokeAPI pokedexes) for Regional Dex order.
- Form types: `""` base, `Other`, `Regional`, `Mega`, `Gmax`, `Dynamax`, `Tera`. Sort rank within a dex number: base, Other, Regional, Mega, Gmax, Dynamax, Tera.
- Rules: Dynamax and Tera attach only to base species or regional forms, never to Other variations. All megas are tagged Kalos; Gmax/Dynamax tagged Galar; Tera tagged Paldea; Meltan/Melmetal region `POGO`.
- Collection state in localStorage `dex-il`: `true` registered, `"p"` pending. UI prefs in `dex-il-ui`. Saved lists `dex-il-lists`. Chosen card art `dex-il-cards`. TCGdex responses cached under `tcg:*` for 7 days.
- Sync codes: `ILDEX{v}.{count}.{base64}`, 2 bits per entry. Version 6 is current; versions 1 to 4 decode against filtered legacy sequences; version 5 is intentionally rejected.

## Interaction model

- Tap row or tile: register / unregister (a pending entry registers on one tap). Long press (a 450ms hold, touch or mouse): toggle Pending; a mouse right click opens the blank-slot menu (below; the 2026-09-06 Pending toggle on right click was removed 2026-09-08 at the owner's ask). Only the click that ends a hold or a swipe is swallowed (`holdId`, cleared by the next pointerdown); the old 600ms window after release also ate a quick follow-up tap on touch, where the hold's own click often never fires. Double click and double tap are unassigned on purpose (a double would delay every single tap). Keyboard: Enter registers, Shift+Enter toggles Pending, arrows move, c opens the card sheet.
- Card lookup sheet (from the strip's lookup button, a list row's card icon, or the c key): every printing of the entry grouped under set headers newest first; menus for sort direction, languages (persisted) and rarity (per list); tap a card for its ledger and "Use in binder as" Registered, Pending or Unregistered, which sets the art and the status together and closes the sheet; a card already in the binder shows Remove from binder instead.
- Tile strip: the card lookup button, a remove X on scan tiles, gen and region chips and the status ball live in a strip over the card's bottom edge (the price is not in it: it floats over the card as a tag and rises above the strip while the strip is up). It slides up on hover (mouse), pins after a sideways swipe across the card (touch: 24px across and mostly sideways; a pointerdown outside the tile or a scroll releases it), and shows on keyboard focus. Card icon (far left of a list row, left of the strip on a tile): open card sheet. The remove X shows only with the strip (hover, swipe, focus) and is dropped under 60px of tile width; the card sheet header has Remove card too.
- Forms menu: type checkboxes only, Base checked by default; an All Forms master row on top checks every kind (partial picks show a dash), unchecking it returns to Base. Entry mode (Separate / Collapsible) and placement (National / Regional / Grouped) live under the gear menu with Manage lists.
- Collapsible mode: `+n` chip on bases with forms; chip color reflects form completion only; drawers are the same row elements, so no state sync is needed.
- Right-edge checkpoint strip: tap to jump, hold and slide to scrub (coarse across regions, fine within one); highlight follows scroll position.

## Data sources

- PokeAPI CSVs (species, pokemon, pokemon_forms, version_groups, pokedexes, pokemon_dex_numbers). Sprites: `scripts/build-sprites.py` builds `sprites/game/` and `sprites/home/` from the official per-game folders of the PokeAPI sprites repo (see the 2026-09-03 sprite pipeline section); named form files are `{pokemonId}-{form}.png`.
- Card lists for Dynamax (VMAX cards without Gigantamax) and Tera (52 Tera Pokemon ex) were taken from Bulbapedia card categories; these need refreshing as new sets release. Tera cards are matched by set name + card number.
- TCGdex API (`api.tcgdex.net/v2/en/`), `dex-ids/{n}` for all cards of a Pokemon, `cards/{id}` for details (G-Max attack detection), `sets` for names.
- Fallback: when TCGdex times out or errors, `api/tcg.js` queries pokemontcg.io (`/v2/cards?q=nationalPokedexNumbers:{n}`, `/v2/cards/{id}`, `/v2/sets`) and maps responses to the TCGdex shape (id, localId, name, image base, rarity, stage/subtypes, set name and release date; attacks and TCGplayer pricing on details). Responses carry `X-Card-Source`. Outage latency controls, in order: a circuit breaker skips the TCGdex probe for 60s after it times out or 5xxes; the fallback query starts in parallel 800ms into a silent probe (hedging); after any fallback-served response the client sends `fb=1` for 60s so every function instance skips the dead probe (the breaker alone cannot cover cold starts); the client never retries TCGdex directly once the proxy has answered; lists are sorted in the function because upstream `orderBy` adds 1.5-3s; set names are fetched in parallel with the card list (memoized `ensureSets`); the detail view renders instantly from grid data (small image upgraded to hi-res on load) and fills rarity/illustrator/price when the fetch lands. Measured with TCGdex healthy: lists 0.2-0.9s cold, detail tap ~20ms visible / ~155ms enriched. During the TCGdex outage via fallback: lists ~3s cold, cached reopens ~0.1s. Card images then come from `images.pokemontcg.io` (`{base}.png` / `{base}_hires.png`); the client's `cimg()` helper handles both hosts. `POKEMONTCG_API_KEY` (optional, raises rate limits) comes from an env var; see `.env.example` and set it in Vercel project env vars.
- `node scripts/check-providers.mjs [proxy-base-url]` reports reachability of both providers in one command, to tell an outage from a bug.

## Known issues / open threads

- 2026-09-08 (fixed, card-binder v2.2.1): the owner reported the binder's page turn still read as two pages turning with a duplicate of the first page in Mac Chrome. Frozen mid-turn in that browser, the turning sheet rendered 1.4x its size and spilled over the site header and past the cover: the component set a fixed 1600px perspective on the book, harmless on the demo's 440px pages and huge on the dex's 590px pages. The perspective now scales with the page width and the book clips at the cover (mechanism in the 2026-09-08 binder customization section). His retry on that build still showed the inside cover twice when opening from the front cover: the landing leaf rendered its inside cover at frame 0 while the cover sheet swung over with the same inside cover on its back; v2.2.2 keeps the landing leaf hidden until the sheet lands. His retry of v2.2.2: "a blank black page turns before the actual binder turns", only from the front or back cover. Measured in his tab: when the book shuts onto a cover it is one page wide at once, the closing sheet starts where the page was (outside that box), and the v2.2.1 clip on the book hid it for the first 40 percent of the turn, so the cover popped into view at the spine; v2.2.3 moves the clip to the host, which spans the full spread in both states. Recorded in a visible Chromium: all four cover turns run as one continuous sheet. His retry of v2.2.3: still a blank page before the board, only when opening from a cover, and the covers lost their outside border in flight. By code: `render()` switched the book to its spread shape at the first frame of an open, so the far half of the cover appeared at once, 160ms before the board started; and the turning sheet was a clone of the leaf alone, while the cover's rim, rounded edge and shadow belong to the book. v2.2.4: an open keeps the book in its shut shape (landing leaf out of the layout) until the board lands, and a cover turn swings the whole board (`.cb-board`). His retry of v2.2.4: fixed, but a light line at the spine centre while a cover turns: the board and the shut book were the leaf plus the cover padding on every side while a half of the open book reaches the spine centre (2px more), so the halves met 4px apart; v2.2.5 gives the shut book and the board a spine-side rim of half the spine. Awaiting his retry of v2.2.5.

- 2026-09-01: full provider outage cycle observed. TCGdex was down all session (ports 80/443 timing out) while pokemontcg.io was up but degraded; the card sheet was verified end to end through the fallback. TCGdex then recovered the same day and the primary path was verified live on the preview too: base/Mega/Gmax/Tera filtering all correct (Gmax = all 4 Charizard VMAX), lists 200-900ms, detail tap visible ~20ms and enriched ~155ms, hi-res image swap working. By then pokemontcg.io had flipped to 500s, so both failover directions are proven. Neither provider is fully healthy; run `scripts/check-providers.mjs` before debugging any lookup issue.
- A failed detail fetch during Gmax/Dynamax classification silently drops the card into the wrong bucket for that load (client treats missing detail as non-Gmax). Successes cache client-side for 7 days, so this self-heals; only matters while a provider is flaky.
- Card lookup failing on iPhone Safari on `MAX-forms`: cross-origin fetch to TCGdex returns "Load failed". The proxy should resolve it; verify with Safari Web Inspector attached to a device once TCGdex is back.
- Build process is string-patching a 2 MB file. Convert to a build script: data JSON (entries, card lists, regional keys) + sprite pipeline + template, emitting `index.html`. Keep entry ids stable.
- Vercel preview URLs require Vercel login (deployment protection on).
- The chat-preview 2 MB limit is no longer a constraint; do not degrade sprites for it.

## Next: build-time card data (decided 2026-09-01)

Replace runtime card-list fetching with build-time static data, modeled on pkmnbindr.com (which bundles its whole card DB as static JSON from its own domain and is outage-proof as a result). Chosen granularity: one JSON per Pokemon (`/data/cards/{dexNo}.json`, ~1025 small files) plus a shared `sets.json`, because the app's only question is "all cards for dex N" at sheet-open. Bake form classification (Gmax/Dynamax/Tera/Mega/regional buckets) into each file at build time, which removes the runtime detail fan-out. Prices stay live through the proxy. Artist browsing (pokemontcg.io `artist` field) is DEFERRED until everything else is done; it becomes trivial once the static data exists. This supersedes most of the proxy fallback machinery for lists.

## 2026-09-01 UI wave (shipped on MAX-forms)

Game Boy Pokedex LIGHT scheme (warm paper ground, black ink, perforated header divider, red accent only on interactive/active; sprites, card art, and region chips keep their colors as content). Compact content-width header; the filter row is filters only (Gen / Region / Forms / Status). Forms = checkboxes ordered Base, Base Variations, Regionals, Megas, Dynamax, Gigantamax, Tera; DEFAULT SHOWS BASE ONLY; entry-mode and placement radios live under the gear with Manage lists. Binder sprite slots mimic a card face: name and dex number header, framed sprite art box, tags directly beneath, a non-interactive Pokeball status in the middle (outline missing, ball collected, tilted pulse pending), card-search icon in the lower-left corner; slots with chosen art show a TCGplayer market-price badge, and list rows show the same price in their empty middle. Final binder page pads with blank slots; binder size = hover grid behind a four-square icon. Pocket view = third view, all tiles in one vertical scroll, columns 2-6 via a one-row picker, touch pinch, or ctrl+wheel trackpad pinch. Tera matching expanded (subtype + set/name alt-art expansion; Tera Charizard = 4 printings). Long-press = Pending on every input; right-click is a reserved no-op.

END-OF-SESSION STATE: a final round (dark mode toggle, redrawn pocket icon, ball resize, pocket/binder style parity) was REVERTED at the owner's call (commits 2816605, b644abb undo 14b6d5f). Also open: merge to main (proposal stands, owner gates), POKEMONTCG_API_KEY not yet set in Vercel env, pocket view lacks the checkpoint strip, iPhone Safari on-device lookup check still owed.

## 2026-09-01 tile polish pass (owner walk feedback, shipped on MAX-forms)

Owner walked the preview and directed a polish pass; this supersedes the tile details in the UI wave block above.

- Binder/pocket tiles are container queries now (`container-type:inline-size` on `.card`): all internal sizing in cqw against the tile, so faces keep real-card proportions at every column count. Tile aspect is 63/88 (true card ratio) with a 5px corner radius. GOTCHA baked into the CSS: cq units on the container's OWN padding resolve against the viewport, not itself, so `.card` padding uses percentages (which resolve against the grid track) while descendants use cqw.
- SUPERSEDED 2026-09-03 (E3 section below). Sprite tiles read as a card face: name + dex number header, framed 10/7 art window, caption strip (form/gen/region, transparent chips, attached under the art frame), Pokeball status centered in the body, hairline footer with the card-search icon. Small tiles shed chrome instead of overflowing: content-box under 80px drops the caption strip, under 62px also drops footer + name (number, art, ball remain).
- SUPERSEDED 2026-09-03 (E3 section below). Chosen-art tiles no longer paint the scan over the whole face: the scan sits contained above the same footer (search icon left, status ball center, TCGplayer price right), so nothing is covered and the slot stays fully interactive. The old cnum/cname/pbadge overlays are gone.
- Pokeball/circle parity fixed at the root: the ball sprite only draws the middle 60% of its PNG (62.5% for the Hisui ball), so it renders via `::after` at 166.8%/160% of the box and the visible ball now equals the empty circle everywhere (tiles and list rows). Pending pulse moved onto the slot element itself.
- Header perforation strip removed (clean 3px ink rule). View/gear icons redrawn (the old pocket icon drew outside its viewBox); columns icon now three tall bars.
- Menus: dropdown panels and popovers get a soft light-theme shadow (the old popover shadows were dark-theme leftovers), and the card-detail primary button was ink-on-ink (unreadable), now ink on paper.
- New: Options (gear) > Clear binder cards, two-tap confirm (arms to "Tap again to clear all", disarms on any click elsewhere); wipes `dex-il-cards` only, registration state untouched.
- The 4+ column region-chip truncation flag is resolved by the compact tiers (the caption strip hides before it can truncate).
- Verified via Playwright at 390/768/1440, binder 2/3/6 columns, pocket 4 columns, list view, last-page blank slots, card sheet open from an art tile, register toggle through art, and the clear-cards flow end to end.

Second owner round, same day:

- SUPERSEDED 2026-09-03 (E3 section below). ONE tile format for every slot, art or not (owner rule: whatever works for the physical-card tile structures all tiles in binder and pocket). Both types end in the same footer: search icon left, status ball center, price right. The ball left the sprite tile's body; the art window now flex-grows (much larger art), with the caption strip reading as the frame's bottom band. Chosen-art tiles are slim-bezel (2% padding, rounded scan corners) per the owner's mock. Tiny tiles (content under 62px) hide search + price but keep the ball, so status survives at 6 columns.
- Options menu no longer renders under the checkpoint strip: header z-index was 10 (a stacking context capping every child below the strip's 25), now 40. Panels got min-width 184px + nowrap labels so entries stop wrapping.
- Binder view icon is a ring binder (rounded cover, spine line, three spine rings); pocket view icon matches the owner's mock (three columns of filled tiles cropped top and bottom, tall middle row, reads as a scrolling card grid).

## 2026-09-02 UI/UX review wave (shipped on MAX-forms)

A five-lane review (visual lens, architecture lens, web-platform research, feasibility and performance verifiers) drove eight commits. Everything below is verified with Playwright at 360/390/768/1440 and a mocked card API; the owner has not walked it yet.

- Layout: one shared column (`--col` 640px, `--gutter` 10px) for header, list, binder and pocket, so every surface shares the same edges. The 40px strip gutter applies to the list only under 720px. Binder and pocket grow with the column count on wide viewports (`min(100%, max(--col, cols * 132px))`, `--cols` set per render). Filter buttons 40px, panel rows, pager, page input, dialog buttons and back-to-top (now clear of the strip) sized for thumbs; the views row and progress bar gave the height back, header stays about 150px at 390.
- Tokens: `--pend`, `--pend-deep`, `--pend-lite`, `--ok`, `--glow`, `--shadow`, `--pogo`, `--icon-search`. Region hues darkened in OKLCH so region chips clear 4.5:1 on the chip ground; form chip is ink; chips 10px; dim sprites 0.6 grayscale. Radios draw as radios. Tile type floors 9px, caption strip sheds at 88px, tiny tiles keep the (shrunk) card-search icon so the sheet stays reachable at 5 and 6 columns; scan tiles carried their dex number in the footer (dropped 2026-09-03 with E3). Pending pulse animates opacity on a painted glow (`::before` on the ball) instead of box-shadow; the found-it flash is the accent.
- Correctness: the sheet has a request token (`cSeq`; close bumps it) so a slow lookup never paints under a newer title; set names never gate the card list; checkpoint runs and scrub targets come from `dispArr` (rendered rows only); long-press click guard is a timestamp scoped to the held id; a pinch or column pick relays out the pocket grid (`layoutPocket`) without rebuilding; Use in binder updates label, tile and row price in place (`refreshTile`); price links are built from the DOM and accept https only; the legacy `tcg:` purge runs once behind `prefs.purged`; on quota `cachePut` evicts the oldest half of `tcg2:` and retries. The fallback hint is the request header `X-Card-Fb: 1` (out of the CDN cache key) and re-arms only when `x-vercel-cache` is not HIT.
- Efficiency: header height cached (`headH`, refreshed by `placeJumps` and resize, also exported as `--hdr` for scroll-margin); the display pass walks `ORDERED` and an `expMap` (dex number to chip) and writes `display` only on change; startup no longer re-appends rows for the default order; the price observer watches only rows with chosen art; tiles register in `bTiles`/`pTiles` maps (no grid scans per tap); under a Status filter an entry that stops matching removes its own row and tile instead of rebuilding; search reuses distance buffers and caches the last hits; lookups share in-flight promises; G-Max classification runs four at a time and reports cards it could not check; one `wireDropdown` sets `aria-expanded`; one document click handler closes everything.
- Intuitive: `#csheet` and `#modal` are `<dialog>` elements (showModal, Escape, focus containment, `html.lock` scroll lock, `overscroll-behavior: contain`, `svh` sizes); loading shows a skeleton grid, errors are centered with the transport detail demoted and a Try again button; Escape also closes dropdowns, pickers, suggestions and the clear confirm. Keyboard: rows and tiles use a roving tabindex (first visible is the tab stop), arrows move, Enter/Space registers, Shift+Enter marks pending, `c` opens the cards; per-row card buttons left the tab order. A press shows a ring growing on the ball for the 450ms hold and clears on drift. Each view remembers its scroll position. Choosing Collapsible entries while only Base is shown enables every form type (the Forms button reflects it). A search jump relaxes only the filters that exclude the target. Suggestions take arrow keys.
- Count line shows the completion percentage.

Not built (verified ideas for later, in order): run marking (long-press then drag to mark a stretch; needs an owner call on what a run paints), checkpoint strip as a progress spine with a scrub bubble, a capture wobble on the status ball (collides with the star burst; pick one), a root cross-fade on view switch only (View Transitions, Safari 18+), a page-turn animation on the existing pager. Rejected on verification: native popover (Safari 17 floor), content-visibility on the list (breaks offsetTop reads the scrub needs), scroll-snap paging, anchor positioning, scroll-driven header bar, pocket-lip pending treatment, ghost-sprite proposal.

DEFERRED: global rarity filter (mark rarities you collect; card sheets show only those). Blocked on rarity data: TCGdex list responses carry no rarity and per-card detail fetches would be 90 requests a sheet. It is the first feature of the build-time card-data pipeline below, where rarity is baked into each file.

## 2026-09-08 binder customization: named cover, contents card, progress band, cover colour, swipe turns, blank slots and pages (owner punch list, shipped on MAX-forms)

- Component: card-binder v2.2.5 (vendored; the component README carries the API; `CardBinder.VERSION` names the running build). The shut front cover wears the name stitched (`.cb-name`: thread-coloured strands painted through `background-clip:text`, a 1px relief shadow); the inside front cover holds a paper contents card (`.cb-contents`: the name, `inside` rows, the progress bar); the inside back cover and shut back cover are blank. Under every page a cover band (`.cb-foot`, `--cb-foot` 18px, counted in the fit calc) carries the page number in the outer corner (`.cb-lp` marks a left-hand page: even page numbers, left corner), the stitched name centred under a left-hand page and the progress bar (`.cb-prog`) under a right-hand page; on the phone each page keeps the band of its side, and the inside front cover is its own page (index -1, pager reads Inside cover) so the contents card is reachable there; a hidden leaf is emptied so no stale tiles linger. `setName`, `setInside`, `setProgress` and `setCover` repaint in place (no pocket is rebuilt), which is what lets `tally()` feed the binder on every tap. `setCover("#rrggbb")` sets `--cb-cover` and `--cb-cover-hi` (`color-mix`) and picks a light or dark `--cb-thread` from the luminance. Opening from the shut front cover now lands on page 1. Page turns move like one sheet (`--cb-turn` .75s; the owner's walk note on the first cut: the leaf-by-leaf motion was too fast and read as two pages turning): the module stages inert ghost copies over the leaves for one turn, a `.cb-flier` with the lifted page on its front and the revealed page on its back swings over the spine and lands on the far leaf while a ghost of the covered page waits underneath; one page at a time the old page lifts away or the previous one drops in; off under `prefers-reduced-motion`. The sheet swings in a perspective the module sets per turn from the page width (`--cb-persp`, ten page widths, so the free edge grows by a tenth at most; v2.2.1, replacing a fixed 1600px that ballooned the dex's wide pages 1.4x and spilled them past the cover) and the host clips (`overflow:clip` on `.cb-host`, which spans the full spread whether the book is shut or open, so a sheet shutting onto a cover stays visible from where the page was; v2.2.3, the v2.2.1 clip on the book cut that sheet off; anything that must pop outside a pocket, like `#ctx`, renders at the document level); a turn started while one is in flight flushes the earlier ghosts, and the ghost safety timeout follows `--cb-turn`. Opening from a cover keeps the book in its shut shape (`layout(was)`: one page wide, same alignment, the landing leaf `hidden`) until the board lands, then the spread appears under it (v2.2.4; v2.2.2 only hid the landing leaf, and the far half of the cover still unfolded at frame 0). A cover turn swings the whole board, `.cb-board`: the leaf inside the cover's rim, rounded edge and shadow, sized like one half of the open book (outer rim `--cb-bpad`, spine side half of `--cb-spine`, the foot band below; `.cb-sl` / `.cb-sr` name the spine side), and on a spread the shut book has that same half-book shape, so shut, in flight and open the halves meet at the spine centre (v2.2.5); a page turn swings the leaf alone. The ghosts are children of the host (`position:relative`, carries `--cb-persp`), positioned from the leaves' rects, so the book can change shape under them. Dex queries of the book's tiles skip `.cb-ghost` subtrees (arrow movement, the edge turn, the first-tile tabindex). A sideways touch or pen swipe of 60px across the book (`touch-action:pan-y` on the book) turns the page; the click that ends a swipe is ignored. The pager is round 40px chevron buttons (36px under 600px) centred under the book.
- Dex feed: `binderName()` is the loaded or saved list's name (`prefs.listName`, set by Save current and Load, cleared by Reset list; default "National Card Dex"); `syncBinderMeta` runs from `tally()` with the contents rows (Game, Set, Entries, Registered, Pending) and the progress fraction plus the header's percent text. The header's binder controls gain a colour swatch (`#cpick`, a native `<input type="color">` behind a round swatch, binder view only; `prefs.cover`), and the gear menu a Reset binder colour entry.
- Arrow keys: `armKeys` walks the container's `.card` or `.row` elements by grid position, stepping over blanks; it takes an edge callback, and in the binder an arrow past the last (or first) tile calls `cb.turn` and focuses the first (or last) visible tile of the new page, or the pager's Next / Previous when the turn shuts the book. A filtered-out entry takes its blanks with it in every view; the strip pin is released on every page turn; `prefs.cover` is accepted only as a hex colour; Reset list also clears the blanks; a pasted sync code or deleting the current list drops the list name.
- Blank slots and pages: `BLANKS` (localStorage `dex-il-blanks`, validated on load) is an ordered list of markers `{blank:true, k:"slot"|"page", b:entryId, at:"before"|"after"}`. A slot blank sits before or after its anchor entry in every view and follows it through filters and re-orders (`slotItems`); a page blank is a whole empty page before or after the page holding its anchor at the current grid (`binderItems`: markers applied in stored order, `per` copies of the marker spliced at the page boundary; a page after a partly filled last page first pads that page with the same marker, so removing the page removes the pad). `indexBinder` builds an entry-to-index map and per-page entry counts once per render for `pageOfEntry` and `entriesThrough`. The binder renders `bItems` (`renderBinder` recomputes it; `setGrid` goes through it so page blanks follow the grid); `pageOfEntry` and `entriesThrough` map entries to pages for the search jump, the scrub strip and the current-region highlight. Blank tiles are `.card.blank` (an empty pocket in the binder, a dashed outline in the pocket view) and blank rows `.row.blank` placed around their anchor rows by `placeBlankRows` at the end of every display pass; both carry their marker in a WeakMap. A mouse right click on an entry offers Blank slot before / after (plus Blank page before / after in the binder); on a blank it offers Remove; the menu is `#ctx` (fixed, closed by any outside click, Escape or a scroll). No touch route yet.
- Verified with Playwright (routed providers, pageerror listener) at 1280x720 and 390x844: the stitched cover, the contents card and its live numbers after a tap, the fit with no document scroll, edge arrows both ways with focus, the swatch and its reset, the four-item menu, a blank page before an entry (nine empty pockets, entries shift a page, 116 sides), the search jump landing on the shifted page, removal, a blank slot after Ivysaur in binder, pocket and list, Escape, naming the binder from a saved list, the phone's single page with the progress band and a one-row pager, a 30px tile swipe pinning the strip without a turn, a 130px swipe turning the page.

## 2026-09-07 binder as the object, desktop width, sheet add-as status (owner walk asks and a reference photo, shipped on MAX-forms)

- Reference: the owner's own binder, a charcoal woven zip binder with no rings, open to a spread of dark pocket sheets with glossy clear sleeves. Built as: `.book` (the cover: charcoal `--cover` with a 1px crosshatch weave from two repeating gradients, an inset hairline, a soft drop shadow), one or two `.leaf` pocket sheets (`--sheet`, page number in the bottom corner from `data-p`), and a `.pkt` sleeve around every tile (`--pocket` backing, a dashed `--seam` outline for the weld, `::before` a static film sheen of one radial highlight plus a diagonal gradient, `::after` the pocket mouth: a light top edge with a shadow under the lip; both `pointer-events:none` so taps reach the card). Empty pockets are `.pkt.empty` at 63/88. Paddings scale per device: `--bpad` (cover), `--lpad` (sheet), `--ppad` (sleeve) are 6px / .7 gap / .3 gap on phones and 16px / gap / .45 gap from 1000px. Under 80px of tile width sprite tiles drop the dex number so the name stays readable.
- Spread: from 1000px wide (`spreadMq`) the book shows two leaves side by side with a shaded spine (`.book.spread::before`), paired like a real binder: the first spread is the inside front cover (`.leaf.cov` holding an `.incover` panel with a paper label) and page 1 on the right, then 2 + 3, 4 + 5, and when the last page lands on the left the inside back cover faces it. `renderBinder` keeps `page` as the right leaf's index (even, 0-based; it may equal the page count when only the back cover is on the right), fills both grids (`bgrid`, `bgrid2`) through one `fill` helper, and the pager steps by two (`pIn` is the left page, `pTot` reads "+ 3 / 114"; the first and last spreads read "1 / 114" and "114 / 114"). Gestures and keys are armed on `#book` so arrows flow across leaves; `updateCurrentJump` counts the right page. Narrower screens keep one leaf and page by one; a media change re-renders.
- Reusable module: the binder was extracted into a standalone zero-dependency component (`card-binder.js` + `card-binder.css`, kept outside this repo in the owner private `sleetcrash/components` repo) with the same behaviour and an `items` + `renderItem` API; see the next bullet for how this file uses it.
- Vendored module (2026-09-08, swap done the same day): the binder is that component, vendored as `vendor/card-binder.js` and `vendor/card-binder.css` (v1.1.0 at the swap, v2.2.5 now; version header on line 1 of each). `index.html` no longer carries binder CSS or paging code: `#book` is the module host, `initBinder(CardBinder)` (called from a `<script type="module">` after the main script, since module scripts run after classic ones) constructs it with `renderItem: e => binderTile(e, bTiles)`, `keys:false` (the tiles own the arrow keys through `armKeys`), the dex label, and an `onChange` that mirrors `page`, prunes `bTiles` of detached tiles, gives the first tile `tabIndex 0`, keeps the header and updates the jump strip. `renderBinder()` syncs `visArr`, the grid size and `page` into the module and re-renders; `goTo(n)` opens the book on a page (search jumps, the scrub strip); `setGrid` calls the module `setGrid`; entering the view calls `close("front")`. The host sets `--cb-reserve` from `--hdr` plus the pager allowance, maps the paper and ink tokens, and `#book .cb-grid` computes `--r` for the tile radius. Gestures stay armed on `#book`. `scripts/vendor-components.sh <components-checkout>` copies the two files in; `--check` reports whether they are behind the checkout. To change binder behaviour: change the component in its repo, release a version, re-vendor here.
- Closed covers (now inside the module; kept as the mechanism record): `closed` ("front" | "back" | null) makes the book one page wide (`.book.closed`, its height computed from the column count in container units so it matches an open page). The binder starts shut on its front cover, right-aligned where page 1 will sit, and shuts on its back cover, left-aligned, past the last left-hand page; `turn(dir)` steps between closed, spreads and closed, `pFirst` / `pLast` go to the covers, a click on the closed book opens it, `goTo` (typed page, entry jump) always opens, and re-entering the binder view starts shut. So the last page always sits alone on the left, an odd page count gets one empty pocket page as the last side (`total`); the pager shows "Front cover" / "Back cover" with the number box hidden.
- Desktop width: `--col` widens to 960px from 1000px (header, list, pocket follow it; pocket tiles are capped near 260px). The binder instead sizes itself from the viewport height: `--tileh` is derived from `100svh` minus the header (`--hdr`), pager and paddings, `--pagew` from the column count, and `--fit` from two pages plus the spine; `max-width:min(100%,1440px,max(360px,var(--fit)))` so a whole spread, pager included, shows without scrolling. More rows on a short window means smaller tiles; that is the intended trade.
- Remove X: it lived on the scan face under a bare `:hover`, which on iPhone applied the sticky hover on the first tap, faded the X in, and WebKit treated the tap as hover-only, so a freshly inserted card would not register. It now lives in the strip beside the lookup button (`.card.scan .strip` is a four-column grid) and appears only with the strip.
- Price: the strip no longer carries a price. The scan tag (`.pprice`) floats over the art clear of the printed border with a drop shadow and moves up above the strip (`bottom:calc(max(22px,13cqw) + 4cqw)`) while the strip is shown.
- Sheet: the action bar is a wrapped row, an uppercase "Use in binder as" label over Registered (primary), Pending and Unregistered; `put(st)` stores the art, calls `setState` when the status changes, refreshes the tile and closes the sheet. Remove from binder repaints the row.
- Verified with Playwright at 1470x812, 1280x720, 1920x1080 and 390x844 (routed providers, pageerror listener): spreads at 3x3, 5x4 and 6x3 fit with no document scroll, paging by two runs cover + 1, 2 + 3, 4 + 5 and ends on 114 + back cover (even count) or 64 + 65 (odd), typed page numbers and entry jumps land on the right spread, an arrow from the last tile of the left leaf focuses the right leaf, a click through the sleeve film registers and the rebuilt tile stays in its pocket, touch tap on a scan tile registers with the X hidden and a swipe exposes it, the price tag clears the strip when pinned, and the sheet's three buttons fit one row at 390.

## 2026-09-06 card lookup: printings by set release, rarity, price, languages (owner asks, shipped on MAX-forms)

- Data: the proxy (`api/tcg.js`) takes `lang` (whitelist of the eleven TCGdex languages with card data) and forwards to `/v2/{lang}/`; the pokemontcg.io fallback and the client's `X-Card-Fb` hint stay English only. `tget(path, lang)` keys the localStorage cache and the in-flight map by language. `scripts/build-sets.py` writes `data/sets.json`, set id to release date per language (about 1,100 TCGdex set-detail requests; rerun when a set ships); the sheet loads it once and fetches `sets/{id}` only for a set the file lacks.
- List: `cardsFor(e, lang)` per selected language (`prefs.clang`, default English), form buckets by name heuristics that hold across languages (VMAX token, Mega prefixes incl. the Japanese one, regional prefixes and suffixes in five languages, G-Max attack prefixes); Tera keys stay English. Cards carry `lang`. `hydrate` fetches each card's detail six at a time (rarity, set, pricing, illustrator, image if the list had none) after the grid renders, updating cells in place; a 2px shell-red line under the menus tracks progress. Details are cached like every other lookup (7 days locally, a day on the CDN), so the second open of an entry is instant.
- Grid: sorted by set release date (`prefs.csort`, newest first by default), then language, set, number; grouped under `.cset` headers (set name, release month, language badge when more than one language is on, count). Each `.ccard` is the bare art with a hairline (radius in container units on the tile), then a mono line with the number and the rarity code and a line with the market price (TCGplayer market in USD when present, else Cardmarket trend in EUR). Rarity codes come from the `RARITY` ladder (TCGdex name to collector code: IR, SIR, HR, UR, RR, VMAX, and so on); the rarity menu lists the codes present in the current list, multi-select OR.
- Detail: art on the paper with a soft shadow beside a ledger (set with date, number of the set's official count, rarity with its code, illustrator, language for non-English cards, TCGplayer market as a link, Cardmarket trend); at 560px and up the two sit side by side. The sticky action bar: Use in binder (shell red) saves the art and closes the sheet; Remove from binder (hairline) keeps it open. Back returns to the grid without refetching.
- Shell: the sheet header is an icon back button, the entry name over a mono "n cards in m sets" line, Remove card (only when art is chosen) and an icon close; the menu row reuses the header's filter idiom (`.filters`, `wireDropdown`), so a set choice underlines in shell red like the main filters.
- Second round (owner walk notes, same day): thumbnails now load as each comes within 900px of the sheet's viewport (the old twelve-at-a-time chain from the top left the middle of a long list blank until the end came into reach); a card whose image arrives only with its detail replaces its name tile (14 Charizard promos have no scan on TCGdex at all and stay name tiles). The price line shows one currency: USD by default, EUR under the gear (`prefs.cur`), converting the other source at the ECB rate of the day (api.frankfurter.app, cached a day in `fx:eurusd`) and marked with an approximation sign; the detail ledger keeps both sources native. Two more menus: Set (the sets in the current list, newest first, release month and language when several are on) and Game (Pokemon TCG / TCG Pocket, persisted as `prefs.cgame`; Pocket sets come from the `tcgp` serie flagged in `data/sets.json`, with the `/tcgp/` image path as a second signal).
- Verified with Playwright at 390 and 1280 against fixture-routed TCGdex responses: date order both directions, rarity narrowing, Japanese merging with badges and persisted prefs, the detail ledger, Use in binder closing the dialog and updating the tile, Back without refetch, no overflow, zero page errors.

## 2026-09-06 tiles: the card and nothing else (owner references: a bare-card binder page and Pokemon TCG Pocket's grid; shipped on MAX-forms)

- Tile: the 63/88 face is the whole tile. Radius all round (`--r`, computed on the grid because container units on `.card` itself resolve against the grid), a hairline plus soft shadow as box-shadow; registered swaps the hairline for `--line-hi`; pending keeps the base hairline (its amber ring was removed 2026-09-08 at the owner's ask, the mailer and the tilted ball carry the status). The bezel row (`.pfoot`, `--bz`) is gone, so `layoutPocket` sets `--tileh` to the face height alone.
- Strip: `.strip` is absolutely positioned over the bottom `max(22px,13cqw)` of the tile, z-index above the mailer, translucent paper, hidden by `translateY(100%)` and slid up over 220ms. Reveal: `.card:hover` under `(hover:hover)`, `.card:focus-visible`, or `.card.pin`. It blurs what it covers (`backdrop-filter`) only while shown so hidden strips cost no compositing layers. Under 110px of tile width the chips drop and the strip keeps lookup and ball (the ball is `justify-self:end`, so it stays on the right edge when the chips leave the grid). Scan tiles carry the price twice: the tag in the face (visible while browsing) and `.sprice` in the strip (visible while the strip covers the tag); both fill from one `priceInfo` call.
- Swipe: `armLongPress` records the pointer type at pointerdown; for a non-mouse pointer on a `.card`, a move of more than 24px across with less than 16px down calls `pinStrip` and sets `holdId` so the click that ends the swipe is swallowed. `touch-action:pan-y` (exactly that value) on the tile keeps vertical scroll native while a sideways move stays with the page: WebKit arms the recognizers that keep a horizontal drag from starting a scroll only for exact pan-y, so the earlier pan-y pinch-zoom could lose a thumb's swipe on iOS. The swipe test is an angle (24px across and more across than down by half again), not a 16px drift cap, because a thumb drifts. One tile is pinned at a time; a document pointerdown outside it or a scroll unpins.
- Right click: the `contextmenu` handler prevents the menu on rows and tiles so a touch long press cannot pop a callout; links and buttons inside them keep the native menu. For a mouse pointer (the last pointerdown's `pointerType`) it opens the blank-slot menu (`openCtx` / `ctxFor`, see the 2026-09-08 binder customization section); from 2026-09-06 to 2026-09-08 it toggled Pending instead, which the owner asked to remove.
- Hold ring: `.card.hold` animates the tile's outline (`holdcard`) instead of the ball's box-shadow, since the ball now sits inside the hidden strip; the list rows keep `holdring` on the slot. The jump flash on a tile is an outline keyframe too (`flashcard`) because the tile no longer has a border to colour.
- Spacing (binder panel superseded 2026-09-07, see the binder section): pocket grid 6px gaps (4px at five or more columns), no panel; binder grid was a paper panel (`--card`, hairline `--line-soft`, 8px radius, faint shadow) whose padding equals its gap: 14px at up to three columns, 10px at four, 8px at five or more. Blank binder slots are dashed placeholders drawn with an inset outline so they measure exactly like a tile.
- Scroll feel: card art and sprites start at opacity 0 (only under `prefers-reduced-motion: no-preference`) until a capturing `load` listener adds `.in`; the existing 200ms opacity transition carries them up. `.card.down` scales the tile to .975 while the pointer is held (120ms down, 300ms back). No holo shine, no parallax.
- Owner walk note, same day: tapping a pending tile registered it but the change looked slow on the phone. Reproduced in the iOS 26 Simulator with an on-page event readout: the status class flipped 3ms after the click, but the tile's first painted frame came 170 to 270ms later (a plain tile: about 25ms). Cause: the ball's `catchpulse` glow (an infinite opacity animation under two drop-shadow filters) kept running inside the hidden strip, and WebKit stalled the repaint when the entry left pending. The tile glow is now static (`.card.pend .pball::before{animation:none;opacity:.6}`, the list row keeps its pulse); measured after the fix: 12ms. Tap-delay theories were checked and dropped: WebKit skips its double-tap wait for any non-auto `touch-action` (`Element::allowsDoubleTapGesture`), so `pan-y pinch-zoom` is fine.
- Verified with Playwright at 390 and 1280 (routed providers, pageerror listener): tile height equals face height in binder and pocket, `--tileh` matches the rendered tile, hover, focus-visible and a CDP touch swipe reveal the strip, vertical and diagonal drags do not pin, scroll and outside taps unpin, tap, long press, Enter and right click set the intended status, the pinned strip's lookup button opens the card sheet, images gain `.in` on first and cached loads, reduced motion keeps everything visible and static.

## 2026-09-05 shell: the Ledger treatment (owner pick S2 from the shell mockups, shipped on MAX-forms)

- Header: the shell band thins to the brand row (title, count in mono, pending count amber, percent) and the progress moves to the header's bottom edge: `.bar` is absolutely positioned across the full header width at `bottom:-1px`, 2px tall, and its `#fill` is shell red, so the sticky header's edge is the progress bar. The 3px ink rule under the header is gone; a hairline (`--line-soft`, oklch 0.8 0.01 95) closes it.
- Search: an underline field (no box) with a search glyph masked at the left (`--icon-find`); the suggestion list is a 4px-radius card with a soft shadow.
- Filter menus: text buttons sized to their label with a chevron (`--icon-chev`), separated by hairlines; the first is flush left. An active or open menu turns ink and an active one carries a 2px shell-red underline. The row wraps when selections widen the labels (no overflow on the row: an overflow rule there clips the absolutely positioned panels). The Gen menu is labelled Generation; its count form stays "n gens" to fit.
- Panels: 4px radius, soft border, hairline rows 42px tall, the checkbox or radio drawn as a trailing check glyph (no box; indeterminate is a dash); group dividers (`.pdiv`) are 8px recessed bands; the gear panel's action rows share the same hairlines. The size and column pickers take the same card.
- Icons: view switcher, size pickers and gear redrawn at 20px with 1.5px strokes; the active view carries the same red underline as an active filter; the gear is a toothed gear (before it read as a sun).
- Checkpoint strip: 64px wide with a hairline on its left, labels are the full region names (`RAIL_NAME` maps only POGO to GO and Gmax to G-Max) in 8.5px tracked capitals, the current one in ink with a 2px shell-red tick on its left edge. List, pocket and binder reserve 64px on the right (was 40px) and the jump pair sits at right:72px.
- Verified with Playwright at 390 and 1280: no horizontal overflow in any view, every rail label fits its 64px, the Forms panel shows all eight rows on a 390 by 844 viewport, a three-gen plus three-region plus status selection fits one filter row at 390, header height 144px feeds the strip's top.

## 2026-09-04 header, navigation and checkpoint strip (owner walk feedback, shipped on MAX-forms)

- Header: still `position:sticky`, now slides away (`header.away`, translateY(-100%), 220ms, none under reduced motion) on a downward scroll and returns on the first upward nudge of more than 6px. It stays put within its own height of the page top, while it holds focus, a hover pointer or an open menu, and for 600ms after any programmatic jump (`keepHeader()`: search jump, checkpoint scrub, pager, view switch) so a jumped-to entry always lands beneath a visible header. On hover-capable pointers a mouse within 16px of the top edge also brings it back. Layout never shifts: the header keeps its space and only translates.
- Shell band (owner pick from the colour mockups, D1): the brand row and the progress bar sit in `header .band`, a full-bleed Pokedex-shell red (`--shell`, oklch 0.5 0.17 25) with the `.hwrap` column inside it; title and count are paper (`--card`), the dim count text is `--shell-dim`, the pending count is `--pend-lite`, and the bar is a paper fill on a `--shell-deep` track (the holographic gradient token is gone with it). Search, filters and views stay on paper below a 6px gap. This is the one deliberate exception to "red only on actionable": the band is identity, not information, and never a control. The page theme colour (meta and manifest) matches the band so browser chrome reads as the same shell.
- Jump buttons: back-to-top and jump-to-end are a stacked pair at the bottom right (top above end), each shown only when at least 500px remain in its direction; `updateJumpBtns()` runs on scroll, resize and every checkpoint re-render.
- Checkpoint strip: previously list-only, now in every view. The runs come from the view's own array (`dispArr` for the list, `visArr` for pocket and binder) and `jumpEl(id)` resolves the row or tile. Pocket scrubs its tiles like the list scrubs rows; binder turns to the page that holds the entry, then scrolls to the tile (clamped by the short page), and highlights the region the page ends in, like a dictionary page header naming the page's last word. The highlight line carries an 8px tolerance so the entry a jump lands 6px under the header counts as reached.
- Pocket placeholder heights: pocket tiles are `content-visibility:auto`, so a never-rendered tile is laid out at its placeholder size. `layoutPocket()` now computes `--tileh` (face plus bezel for the current column width; the tile border is added on top of the intrinsic size, so it is excluded) and the grid uses `contain-intrinsic-size:auto 100px auto var(--tileh)`. Before this the placeholders were 160px against real tiles of about 240 to 290px, so any deep scroll target was wrong until layout caught up. Verified: placeholder height equals rendered height exactly, and checkpoint presses land Treecko and Sprigatito at header plus 6px on a 390px viewport.
- Status ground (owner walk, same day, round four): the sprite face's name row and the bezel share one height (`--bz`, `max(18px,11cqw)`, up from an 8cqw bezel so the chips and ball have room) and both are paper in every state (the bezel paints `--card` itself rather than showing the tile ground, owner ask 2026-09-04). Status is painted on the ground behind the sprite (`--art`, also the list row's sprite box) and on the border: mid gray (oklch 0.74, darker than the page well so grayscale sprites read and registered tiles stand out) for missing, the TCG energy colour of the entry's first type for registered (owner follow-up, same hour; dark oklch 0.3 is the fallback for an untyped id), amber for pending, pulsing in step with the ball's 1.1s glow together with the face hairline while the border pulses between the two pending ambers. Ground changes cross-fade over 300ms; every pulse stops under reduced motion. Blank slots and `--tileh` follow the new bezel. (A first cut painted the bands instead of the ground; the owner corrected it the same hour.)
- Slot status, third pass (owner call, same day, third session): missing is grayed out on both tile kinds (a scan now renders at grayscale and 0.55 opacity like a missing sprite, via `.cardart`; registered restores full colour), registered stays full colour on its energy ground, and pending no longer pulses: the face is covered edge to edge by the black poly bubble mailer the card ships in (owner reference, a card-sized black bubble mailer; a first cut was a tilted kraft envelope with a rocking transform and a type stamp, all three dropped at his walk): `.mail` paints the poly (`--poly`) with a honeycomb of bubble rings (two offset radial-gradient layers on a 9cqw by 15.6cqw cell, rings in `--poly-hi`) under a raking-light gradient, `.flap` is the sealed flap folded over the top: a smooth 16 percent band of the same poly with a highlight along the fold and a shadow seam under it (an earlier cut showed the open flap's paper peel liner; the owner asked for a closed mailer), and the contents differ by tile kind (owner ask): every mailer gets one paper `.lbl` with the sprite thumbnail, the name and No.dddd (the barcode was dropped to keep the sticker short); a chosen card also shows through a sticker-sized plastic window cut in the mailer above the label (owner asks: the window is the label's footprint, and on a card mailer the label is positioned absolutely, centred between the window's bottom edge and the mailer's bottom edge, `.card.scan .lbl`; `.card .face` holds `--wx`, `--wt` and `--wh`, the label takes its width and height from them, `.card.scan .mail` is clipped with an evenodd polygon hole at the same box so the poly reads as a thick border, and a sibling `.win` draws the film: bright rim, inner shadow under the cut edge, a glossy diagonal highlight and a faint milky tint over the scan, which renders in full colour while pending). The 80px tier narrows the panel and shortens the box with the label's smaller content. Static; the amber face and border pulses and their keyframes are gone, the border stays static amber and the ball keeps its glow. The mailer is inserted into the face when an entry turns pending and removed when it leaves (`syncMailer`, called from the tile builder and from `syncEntryUI`), so only pending tiles carry the markup; a failed thumbnail degrades to the dex number like a sprite. Under 80px slots the label drops the thumbnail and name and keeps the number. This replaced the planned sleeve-and-pocket slot rebuild; the slot geometry, bezel, blank slots and `--tileh` are unchanged.
- Energy table: `scripts/build-types.py` reads PokeAPI's `pokemon_types.csv` and `types.csv` (cached in `scripts/.sprite-cache/`), takes each entry's first type by its own id, then its base-sprite id, then its species, maps game types to the modern energy set (bug to Grass, ice to Water, ghost and fairy to Psychic, rock and ground to Fighting, poison to Darkness, flying and normal to Colorless, no Fairy energy) and rewrites the `const ENERGY=` line in index.html (ten code-to-id lists, about 8 KB). Runtime builds `energyOf` from it and every tile and list row carries an `e-<code>` class whose `--e` token is the ground colour (pastel: lightness 0.84 to 0.92, chroma 0.07 to 0.09, Darkness a step darker at 0.72, Colorless near paper at 0.97 so it reads as white against the missing gray; the owner asked for muted over the first saturated cut). Result: 1,602 typed, 0 untyped, 278 through a fallback (the 98 base-sprite entries plus app-only form ids). Regional forms take their own types (Alolan Rattata is Darkness, Galarian Ponyta is Psychic); Tera and Dynamax entries take the base species type, so a Tera card whose printed energy differs is a known simplification. Typing is the CURRENT game typing, not the debut typing (owner call: Clefairy is Psychic-ground because it is pure Fairy since Gen 6, Jigglypuff is Colorless because Normal comes first); switching to debut typing would read `pokemon_types_past.csv` in the same script.
- Tile scale (owner walk, same day): every part of a tile (name, number, bezel, chips, ball, search icon, price tag, remove button) is sized in slot units with a legibility floor and no ceiling, so a one-column tile is the four-column tile scaled up rather than a big card with 24px furniture. Measured: bezel 8 percent, ball 6.5 percent and name 7.5 percent of tile width at 578px and 328px; the floors engage below about 200px exactly as before. `--tileh` follows the same uncapped bezel.
- Pocket and binder reserve the strip's 40px on the right at every width (a six-column pocket left under 40px between 721 and 851px), so the strip never covers a tile.
- Under 720px the pocket and binder sections carry the list's extra 40px right padding so the strip never covers a tile; the rule sits after the section's base rule (same specificity, later wins).
- Verified with Playwright at 390 and 1280: header hides after a 600px wheel, returns on an 80px upward wheel, stays with a menu open, stays after a search jump with the row at header plus 6px, returns on a mouse at the top edge; picker sits 2px left of the gear on the same row with its popover inside the viewport; jump pair stacked with a 2px gap, end button scrolls to the document end and the pair swaps visibility; pocket and binder strips render 11 regions with correct highlights; no page errors in any view.

## 2026-09-03 E3 slot format (owner pick, shipped on MAX-forms)

The owner reviewed six slot formats and chose E3: no top bezel, a full-width card face, one bezel strip under it. Every binder and pocket slot now has this shape.

- Geometry: the slot has no aspect ratio of its own. The face is `width:100%; aspect-ratio:63/88` (`.face`), so a physical card scan fills it exactly (`object-fit:cover`, which trims under 1% off a provider image whose ratio differs from 63/88). The bezel (`.pfoot`) is a strip of `max(16px,8cqw)` added below the face, so the slot is taller than a bare card by exactly the bezel. Sprite slots and scan slots share the outer size; blank slots (`.blankc`) reproduce it with a 63/88 block plus a bezel-height block.
- Bezel: three-column grid, card-search button left, tags centred (form, gen, region chips), status ball right. Ball is `max(12px,6.5cqw)` so it sits inside the strip.
- Face edge (owner follow-up 2026-09-04): the face's bottom corners are rounded to the card radius (`4cqw` of the slot, matching the physical card) and the face draws its own hairline as a 1px `box-shadow`, so the line curves up around the card's corners and the bezel paper fills the gap under them; the bezel strip has no top border of its own. The radius is set in slot units on the face, not `var(--r)`: the slot is a container, so container units inside it resolve against the slot, not the grid.
- Price (same follow-up): on scan tiles the TCGplayer price is a tag over the bottom-right corner of the card, inside the face (`.pprice`, inset 2cqw, ink ground, paper monospace text, 1px paper ring so it reads on any art), still a link to TCGplayer; a tap on it opens the link and does not cycle status.
- Sprite face: name and dex-number header on paper with a hairline under it, art window below on the well ground, all inside the 63/88 face. Scan face: the image, plus a remove X button (`.rmx`) top-right that is invisible and inert at rest and appears on hover or `:focus-within` (a first tap on touch focuses the tile and cycles status; the second tap on the X removes the art). `removeArt(id)` is shared by the X and the Remove card button in the sheet header.
- Narrow slots shed content by container width: the price tag hidden under 80px, the Gen chip hidden under 120px when a form chip is present and under 100px always, all tags hidden under 80px, name hidden and search icon shrunk under 62px. Verified fit (no clipped bezel text) at 390/2, 390/3, 390/6, 768/4, 1440/3, 1440/6 columns and pocket 2 columns at 390.
- Verified with Playwright against the served file with the scan image routed locally: face equals slot width at every width, ratio 0.716, bezel 16px flush at the bottom, sprite and scan slots equal height, blank slots equal tile height, no horizontal overflow, no page errors; hover shows the X, a body tap still cycles status, the X removes the art and leaves status untouched, the search icon opens the sheet.
- Not carried over from the older format: the dex number on scan tiles (E3 as drawn has no room; C1 and C2 were the identity variants the owner did not pick).
- Corners (owner follow-up, same day): slots and blank slots are rounded to match a physical card, the one exception to the square-corner rule. Radius = 4% of slot width, fitted on the pokemontcg.io hi-res image (its transparent corner radius is about 30px on 734px) and close to the physical 63mm card; TCGdex images are square-cornered crops, so the slot clips them to the same shape. The grids (`.bgrid`, `.pgrid`) are inline-size containers and set `--r` from `100cqw`, `--cols` and `--gap` (the gap is now the variable the render functions set), so the radius tracks the real column width with no JS measurement. A scan face has no background so its own rounded bottom corners show slot paper, not the well.
- Size pickers (owner follow-up 2026-09-04): the binder-size and pocket-column pickers moved out of the sticky `.bsize` bars into the header's views row, immediately left of the options gear, styled as the row's other icons; `setView` shows only the active view's picker. The header's own hide-and-return behaviour (below) keeps them reachable on a long pocket, and no tile corner is covered any more.

## 2026-09-03 sprite pipeline: official per-game sprites, HOME option (shipped on MAX-forms)

The owner found that the Black/White-style sprites for everything after Gen 5 (and all Mega, G-Max and regional forms) were Smogon fan art served through PokeAPI, and wants only official sprites. Every entry is now drawn by the fullest official game set of its own generation, with Pokemon HOME renders as a user-selectable alternative.

- `scripts/build-sprites.py` (Python, Pillow with WebP) builds `sprites/game/{appId}.webp` and `sprites/home/{appId}.webp` from the PokeAPI sprites repo. Game set by generation of introduction: 1 FireRed/LeafGreen, 2 HeartGold/SoulSilver, 3 Emerald, 4 Platinum, 5 Black/White, 6 Omega Ruby/Alpha Sapphire, 7 Ultra Sun/Ultra Moon, 8 and 9 HOME (PokeAPI has no full Sword/Shield or Scarlet/Violet set, only menu icons). A form missing from its own set walks forward through later official sets, then HOME; since only same-or-later sets are eligible, the BW folder is only ever read for Gen 1 to 5 entries, which is what keeps its fan sprites (ids above 649) out. Stems are tried most-specific first across all sets (`{pokemonId}-{form}`, then the pokemon id, then the species), so Spiky-eared Pichu comes from HGSS rather than plain Pichu from Platinum.
- Result: 1,504 game sprites (FRLG 151, HGSS 126, Emerald 141, Platinum 140, BW 175, ORAS 178, USUM 157, HOME 436) and 1,504 HOME sprites; 98 entries (Dynamax, Tera, Surfing and Flying Pikachu) reuse their base sprite through the entry's 7th field. Known fallbacks, all reported by the script: the six Cosplay Pikachu costumes have no official sprite anywhere and show base Pikachu; HOME lacks Spiky-eared Pichu. Pixel sets are stored lossless; HOME renders are resized to 160px, quality 85. Sizes: game 8.4 MB, home 13 MB. Downloads cache in `scripts/.sprite-cache/` (gitignored); 16 GitHub API calls per run (set `GITHUB_TOKEN` if rate-limited).
- Runtime: the 2 MB embedded base64 `SPR` blob is gone (index.html 2.2 MB to 186 KB). `sprSrc(id)` picks `sprites/game` or `sprites/home` from `prefs.sprites`; `sprImg(sid)` renders the tag and adds class `sm` (smooth rendering) when the sprite is a 3D render: HOME mode, generation above 5 (via `genOf`, a map built from `DEX`), or an id in `SPRSMOOTH` (the build script patches that line; currently empty because every Gen 1 to 5 entry found a pixel sprite). Everything else keeps `image-rendering:pixelated`. Both helpers sit right after `prefs` because the list rows are built before `byId` exists. A sprite that fails to load is swapped for its dex number by one capturing `error` listener.
- Options gains a Sprites pair of radios (Game sprites, default; HOME renders). Changing it swaps every list row's image in place and re-renders the open binder or pocket view.
- Verified with Playwright against the served file: 36 of 36 tiles loaded on nine binder pages spanning every region (Kanto through Paldea), smooth class 0 for Gen 1 to 5 pages and 36 for Gen 6 onward, HOME mode loads 36 of 36 with every image smooth, the toggle rewrites all 60 sampled row images to `sprites/home` and persists, Dynamax Pikachu and Tera Charizard resolve to the base files, a 404 sprite degrades to `#4`, no page errors.
- Open: HOME renders at 160px are soft on a one-column pocket on a phone (art window about 220px); bump `HOME_PX` to 200 or 224 if the owner notices, at about 50 percent more bytes for the HOME set. Symbols for Dynamax and Tera (no distinct render exists) are not built yet.

## Roadmap (from README)

Male/female variants; per-game regional dex with native numbering; card-as-binder-image polish; account-free encrypted sync (pairing key + Vercel KV); PWA (manifest + service worker) for installable offline use; GitHub Releases for a downloadable copy; camera/photo card lookup.

## Testing

A jsdom harness was used for logic (tap state machine, filters, ordering, sync codes, search). Anything touching layout, gestures, or the network needs a real browser; prefer Safari on device or the iOS Simulator.
